stubs:
 send: backend(Claim) -> Claim                       | retrieve
 retrieve: elastic(Claim) -> ClaimWithLines          | rerank
 rerank: scorer(ClaimWithLines) -> ClaimWithLines    | label
 label: inference(ClaimWithLines) -> EvaluatedClaim  | receive
 receive: backend(EvaluatedClaim)                  ->|