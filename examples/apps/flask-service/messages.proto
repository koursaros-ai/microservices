syntax = "proto3";

message Claim {
    string id = 1;
    string text = 2;
}

message ClaimWithLines {
    Claim claim = 1;
    repeated string lines = 2;
}

message EvaluatedClaim {
    Claim claim = 1;
    repeated string evidence = 2;
    string label = 3;
}