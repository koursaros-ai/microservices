stubs:
 send: backend.send_sentence(Claim) -> Claim                 | retrieve
 retrieve: elastic.get_articles(Claim) -> ClaimWithLines     | score
 score: scorer.rank(ClaimWithLines) -> ClaimWithLines        | receive
 receive: backend.receive(ClaimWithLines)                  ->|