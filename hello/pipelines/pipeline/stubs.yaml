stubs:
 send: service.request() -> Notification                    | worker
 worker: worker.process(Notification) -> Notification       | receive
 receive: service.receive(Notification)                   ->|